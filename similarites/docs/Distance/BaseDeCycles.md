
Base de cycle : Ensemble minimal (au sens de l'inclusion) de cycles avec lesquels on peut générer tous les autres cycles du graphe
Base de cycle minimum : Base de cycle où la somme de la longueur des cycles est minimale (unique askip)

Base de cycle minimum se calcule en temps polynomial.

On met les labels sur les cycles et le graph kernel serait les cycles en commun (DICE/Jaccard ? Jsp ça doit avoir un nom)